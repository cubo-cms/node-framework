{
  "route": {
    "GET /": { "dataType": "Article", "id": "home" },
    "GET /favicon.ico": { "method": "skip" },
    "GET /${dataType}": {},
    "GET /${dataType}:${id}": {},
    "GET /${dataType}/:${id}": {},
    "GET /${dataType}/${filter}:${id}": {},
    "GET /${dataType}/${filter}/:${id}": {},
    "POST /User/authenticate": { "dataType": "User", "method": "authenticate" },
    "GET /api/${dataType}": { "render": "json" },
    "GET /api/${dataType}:${id}": { "render": "json" },
    "GET /api/${dataType}/:${id}": { "render": "json" }
  }
}
